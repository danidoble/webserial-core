/**
 * @file index.ts
 * WebSocket adapter — re-exports `createWebSocketProvider` as the public API
 * for relaying serial communication over a WebSocket bridge server.
 */
export { createWebSocketProvider } from "./WebSocketProvider.js";
